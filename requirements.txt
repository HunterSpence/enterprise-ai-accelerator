# enterprise-ai-accelerator — Root shared dependencies
# Used by CI, the demo runner, and local development.
# Individual modules have their own requirements.txt for Docker builds.

# AI — Opus 4.7 requires anthropic SDK with extended thinking, batches,
# citations, files, prompt caching, and native tool-use support.
anthropic>=0.69.0

# Web framework (shared by all API modules)
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# Data validation
pydantic>=2.7.0
pydantic-settings>=2.2.0

# HTTP client
httpx>=0.27.0

# Terminal UI
rich>=13.7.0

# Analytics / ML (finops + cloud_iq)
numpy>=1.26.0
scikit-learn>=1.4.0
pandas>=2.2.0
duckdb>=0.10.3

# Cloud SDKs (cloud_iq)
boto3>=1.34.0
botocore>=1.34.0

# Kubernetes (cloud_iq)
kubernetes>=29.0.0

# Async / observability
structlog>=24.1.0
websockets>=12.0

# Database (policy_guard)
asyncpg>=0.29.0
sqlalchemy[asyncio]>=2.0.30

# Templating (policy_guard reports)
jinja2>=3.1.4
python-dateutil>=2.9.0
python-multipart>=0.0.9

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0

# GitHub App JWT signing (integrations/github_app.py)
PyJWT>=2.8.0
cryptography>=42.0.0

# Linting / type-checking (dev)
ruff>=0.4.0
mypy>=1.10.0

# Multi-cloud discovery adapters (cloud_iq/adapters/)
azure-identity>=1.16.0
azure-mgmt-resourcegraph>=8.0.0
azure-mgmt-costmanagement>=4.0.0
google-cloud-asset>=3.25.0
google-cloud-billing>=1.13.0

# Observability — OpenTelemetry + Prometheus (opt-in; all OSS, self-hostable)
opentelemetry-api>=1.27.0
opentelemetry-sdk>=1.27.0
opentelemetry-exporter-otlp>=1.27.0
opentelemetry-instrumentation-fastapi>=0.48b0
opentelemetry-instrumentation-httpx>=0.48b0
opentelemetry-instrumentation-asyncpg>=0.48b0
prometheus-client>=0.20.0

# IaC Security + SBOM + CVE (iac_security/)
python-hcl2>=4.3.3
cyclonedx-python-lib>=7.3.0
packageurl-python>=0.15.0
