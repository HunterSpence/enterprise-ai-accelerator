# AIAuditTrail V2 — Runtime Dependencies
# Python 3.12+
# Installs the API server + Anthropic integration extras from pyproject.toml
# Core module has zero mandatory deps (stdlib only).

# REST API
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.0.0
websockets>=12.0

# AI SDK integrations
anthropic>=0.40.0

# Terminal UI / reporting
rich>=13.7.0

# HTTP client (tests + integrations)
httpx>=0.27.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
coverage[toml]>=7.4.0
