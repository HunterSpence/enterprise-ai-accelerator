# FinOps Intelligence V2 — Python 3.12 dependencies
# Install: pip install -r requirements.txt
# Optional GPU/LSTM: pip install torch --index-url https://download.pytorch.org/whl/cpu

# --- Core API ---
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0
pydantic-settings>=2.2.0
httpx>=0.27.0         # async HTTP client, FastAPI test client

# --- Analytics ---
duckdb>=0.10.3
pandas>=2.2.0
numpy>=1.26.0

# --- ML / Forecasting ---
scikit-learn>=1.4.0   # IsolationForest
prophet>=1.1.5        # time-series forecasting
# torch>=2.2.0        # optional LSTM autoencoder — install separately

# --- AI ---
anthropic>=0.27.0     # Claude Haiku/Sonnet for NL interface + narrative

# --- Terminal UI ---
rich>=13.7.0

# --- Caching ---
redis>=5.0.0          # optional query cache — skipped if REDIS_URL not set

# --- Testing ---
pytest>=8.1.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0

# --- Linting / Type-checking (dev only) ---
ruff>=0.4.0
mypy>=1.10.0
